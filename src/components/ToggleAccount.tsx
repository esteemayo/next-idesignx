'use client';

import styled from 'styled-components';

const ToggleAccount = () => {
  return <Container>ToggleAccount</Container>;
};

const Container = styled.div``;

export default ToggleAccount;
