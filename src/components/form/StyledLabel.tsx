import styled from 'styled-components';

export const StyledLabel = styled.label`
  display: block;
  font-weight: 400;
  font-size: 1.4rem;
  color: var(--clr-gray-dark-2);
  margin-bottom: 1rem;
`;
