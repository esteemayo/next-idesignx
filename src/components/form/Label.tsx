import styled from 'styled-components';

export const Label = styled.label`
  display: block;
  font-weight: 400;
  font-size: 1.4rem;
  color: var(--clr-gray-dark-2);
  margin-left: 2rem;
  margin-top: 0.7rem;
`;
